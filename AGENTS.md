
* Use `bin/rails generate` when possible 
* Use DaisyUI componenet https://daisyui.com/llms.txt
* Use Bootstrap icons https://icons.getbootstrap.com/#icons 
* Use `GET /dev/login` with playwright to connect to the first User in the db.
